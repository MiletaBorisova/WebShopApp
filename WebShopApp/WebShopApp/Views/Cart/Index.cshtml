@model WebShopApp.Infrastructure.Data.Entities.Cart

<div class="container my-5">
    <h2>Моята количка</h2>

    @if (Model.Items == null || !Model.Items.Any())
    {
        <p>Количката е празна.</p>
    }
    else
    {
        <table class="table align-middle">
            <thead>
                <tr>
                    <th>Продукт</th>
                    <th>Цена</th>       <!-- единична цена с отстъпка -->
                    <th>Количество</th> <!-- бутоните + и - -->
                    <th>Общо</th>       <!-- Price * Quantity -->
                    <th></th>           <!-- бутон за премахване -->
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>
                            <img src="@item.Product.Picture" width="60" class="me-2" />
                            @item.Product.ProductName
                        </td>

                        <!-- Цена на единица -->
                        <td>
                            @item.Price лв.
                        </td>

                        <!-- Количество с бутоните +/- -->
                        <td>
                            <div class="d-flex align-items-center">
                                <a asp-action="DecreaseQuantity"
                                   asp-route-productId="@item.Id"
                                   class="btn btn-sm btn-secondary me-2">-</a>

                                <span>@item.Quantity</span>

                                <a asp-action="IncreaseQuantity"
                                   asp-route-productId="@item.Id"
                                   class="btn btn-sm btn-secondary ms-2">+</a>
                            </div>
                        </td>

                        <!-- Общата сума за реда -->
                        <td>
                            @(item.Price* item.Quantity) лв.
                        </td>

                        <!-- Бутон за премахване -->
                        <td>
                            <a asp-action="Remove"
                               asp-route-productId="@item.Id"
                               class="btn btn-danger btn-sm">
                                ✕
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        <!-- Общо за количката -->
        <h4 class="text-end">
            Общо: @Model.Items.Sum(i => i.Price * i.Quantity) лв.
        </h4>

        <div class="text-end">
            <a class="btn btn-success" asp-controller="Order" asp-action="Create">Поръчай</a>
        </div>
    }
</div>
